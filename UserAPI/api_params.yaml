environments:
  - name: dev
    endpoints:
      production:
        url: ${DEV_EI_PROD_URL}/userIntegration/api/users
        config:
          retryTimeOut: $RETRY
      sandbox:
        url: http://${DEV_EI_SAND_URL}/userIntegration/api/users
        config:
          retryTimeOut: $RETRY        
  - name: prod
    endpoints:
      production:
        url: http://prod.ei.com:8283/userIntegration/api/users
        config:
          retryTimeOut: $RETRY      
      sandbox:
        url: http://prod.ei.sandbox.wso2.com:8283/userIntegration/api/users
        config:
          retryTimeOut: $RETRY 