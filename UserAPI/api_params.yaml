environments:
  - name: dev
    endpoints:
      production:
        url: http://dev.wso2.com:8595/api/users
        config:
          retryTimeOut: $RETRY
      sandbox:
        url: http://dev.sandbox.wso2.com:8595/api/users
        config:
          retryTimeOut: $RETRY        
  - name: prod
    endpoints:
      production:
        url: http://prod.wso2.com:8595/api/users
        config:
          retryTimeOut: $RETRY      
      sandbox:
        url: http://prod.sandbox.wso2.com:8595/api/users
        config:
          retryTimeOut: $RETRY 